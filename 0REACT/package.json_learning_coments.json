
{
  // --- 1. IDENTIFICATION & METADATA ---

  // 'name': (REQUIRED) The name of your package/project. Must be lowercase and hyphen-separated.
  // This is used when publishing to npm.
  "name": "my-learning-project",

  // 'version': (REQUIRED) The current version of your project, following Semantic Versioning (SemVer: MAJOR.MINOR.PATCH).
  // E.g., 1.0.0 (initial release), 1.0.1 (bug fix), 1.1.0 (new feature).
  "version": "1.0.0",

  // 'description': A brief, human-readable summary of what your project does.
  "description": "A comprehensive package.json example for educational purposes.",

  // 'keywords': An array of strings that helps people find your package on npm search.
  "keywords": [
    "javascript",
    "learning",
    "node",
    "react-example"
  ],

  // 'author': The person or entity who created the package.
  "author": "Learner McCode (https://github.com/Learner)",

  // 'license': Specifies the license under which your project is released (e.g., MIT, ISC, GPL-3.0).
  "license": "ISC",

  // 'private': If set to true, npm will refuse to publish it. Essential for applications that are not libraries.
  "private": true,


  // --- 2. FILE LOCATION & ENTRY POINTS ---

  // 'main': The primary entry point to your module (for Node.js environments or bundlers).
  // If someone 'require()'s your package, this is the file that is loaded.
  "main": "dist/index.js",

  // 'module': Specifies the main entry point for ES Module compatible bundlers (like Webpack or Rollup).
  "module": "dist/index.es.js",

  // 'type': Specifies the module system. "commonjs" is the default (using require/module.exports).
  // "module" enables ES Modules (using import/export) natively in Node.js.
  // "type": "module", 

  // 'files': An array of file patterns that should be included when your package is installed or published.
  "files": [
    "dist/",
    "src/",
    "LICENSE",
    "README.md"
  ],

  // 'repository': Specifies where the source code lives. Helps with cloning and tracking.
  "repository": {
    "type": "git",
    "url": "https://github.com/Learner/my-learning-project.git"
  },

  // 'homepage': The URL to the project's documentation or website.
  "homepage": "https://github.com/Learner/my-learning-project#readme",


  // --- 3. SCRIPTS ---

  // 'scripts': A dictionary of custom commands that can be run with 'npm run [script-name]'.
  "scripts": {
    // Standard commands
    "start": "node src/index.js", // Command to start the application (e.g., in development).
    "build": "babel src --out-dir dist", // Command to compile or bundle assets (e.g., Babel/Webpack).
    "test": "jest", // Command to run automated tests.
    "lint": "eslint src/", // Command to check code for style and errors.
    "prepublishOnly": "npm run build", // Runs BEFORE a package is packed and published to npm.

    // Example of combining commands (runs 'lint' then 'test')
    "validate": "npm run lint && npm run test"
  },


  // --- 4. DEPENDENCIES ---

  // 'dependencies': Packages required for the application to run correctly in **production** (runtime).
  // The version syntax uses common range operators (e.g., ^, ~, >).
  "dependencies": {
    "express": "^4.17.1", // Caret (^): Allows minor version updates (safe bug fixes/features).
    "react": "18.2.0", // Fixed version: Only this specific version will be used.
    "lodash": "~4.17.21" // Tilde (~): Allows patch version updates (safe bug fixes only).
  },

  // 'devDependencies': Packages required only for **development and testing** (not needed at runtime).
  "devDependencies": {
    "@babel/cli": "^7.19.0",
    "@babel/core": "^7.19.1",
    "jest": "^29.2.1", // Testing framework
    "eslint": "^8.25.0", // Linting tool
    "webpack": "^5.74.0" // Bundler
  },

  // 'peerDependencies': Dependencies your package needs, but expects the *consuming project* to install.
  // Common in plugins or UI libraries (e.g., a React library expects React to be installed).
  "peerDependencies": {
    "react": ">=16.8.0"
  },

  // 'optionalDependencies': Dependencies that are used if they are available, but your package can still function if they are not installed.
  "optionalDependencies": {
    "fsevents": "*" // Often used for faster file watching on macOS (but not needed on Linux/Windows).
  },


  // --- 5. CONFIGURATION & TOOLING ---

  // 'engines': Specifies the compatible versions of Node.js and npm your project requires.
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },

  // 'browserslist': Used by tools like Babel, PostCSS, and Autoprefixer to target specific browsers.
  "browserslist": [
    ">0.2%", // Target browsers with >0.2% market share
    "not dead",
    "not op_mini all"
  ],
  
  // 'config': Custom configuration options for scripts.
  "config": {
    "port": "3000"
  }
}